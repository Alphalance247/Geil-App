const Button = ({ onClick }) => {
  return (
    <button
      className="px-6 py-2 text-[#1F1F1F] font-bold text-sm bg-[#6BD051] rounded-[30px] font-geist"
      onClick={onClick}
    >
      CONTACT US
    </button>
  );
};

export default Button;
